<div class="post-paging">
    <% if(page.prev) {%>
    <a href="<%- url_for(page.prev.path) %>">
        <div class="post-paging-last">
            <span><%- theme.pagination.previous %></span>
            <p><%= page.prev.title %></p>
        </div>
    </a>
    <% } %>

    <% if(page.next) {%>
    <a href="<%- url_for(page.next.path) %>">
        <div class="post-paging-next">
            <span><%- theme.pagination.next %></span>
            <p><%= page.next.title %></p>
        </div>
    </a>
    <% } %>
</div>